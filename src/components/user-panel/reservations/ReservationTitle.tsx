'use client'
import { Card, Group, Title } from '@mantine/core'

export const ReservationTitle = () => {
  return (
    <Card>
      <Group justify="space-between">
        <Title order={1}>Rezerwacje</Title>
      </Group>
    </Card>
  )
}
